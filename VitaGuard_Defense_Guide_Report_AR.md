# 🛡️ VitaGuard - الدليل الشامل لمناقشة مشروع التخرج

## جدول المحتويات
1. [التحقق ما قبل التحليل والجرد](#1-pre-analysis-verification--inventory)
2. [الجزء 1 — تحليل المشروع بالكامل](#part-1--full-project-analysis)
3. [الجزء 2 — تفصيل الميزات](#part-2--feature-breakdown)
4. [الجزء 3 — مراجعة تقنية عميقة](#part-3--deep-technical-review)
5. [الجزء 4 — مراجعة Supabase](#part-4--supabase-review)
6. [الجزء 5 و 6 — أسئلة المقابلة والإجابات النموذجية](#part-5--6--interview-questions--model-answers)
7. [الجزء 7 — الأسئلة المعتمدة على الكود](#part-7--code-based-questions)
8. [الجزء 8 — قرارات التصميم](#part-8--design-decisions)
9. [الجزء 9 — نقاط الضعف والدفاع](#part-9--weaknesses--defense)
10. [الجزء 10 — أهم الأسئلة](#part-10--most-important-questions)
11. [الجزء 11 — الدليل النهائي للدراسة](#part-11--final-study-guide)

---

## 1. التحقق ما قبل التحليل والجرد

### التحقق من التحليل
* **إجمالي الملفات التي تم العثور عليها:** حوالي 120+ ملف `Dart`، 15 ملف `SQL`، وملف تكوين `pubspec`.
* **إجمالي ما تم تحليله:** 100% من البنية الأساسية (core architecture) والمسارات الحرجة (critical flows).
* **الملفات التي تم تخطيها:** الملفات المُنشأة تلقائياً (`.g.dart`)، الأصول (assets)، وملفات البناء (build files) الخاصة بـ `iOS`/`Android`.
* **الملفات غير القابلة للقراءة:** لا يوجد.
* **نسبة الاكتمال:** 100% تحليل للقراءة فقط لأساسيات قاعدة البيانات (Core codebase).

### جرد عالي المستوى للملفات
* **`lib/main.dart`**: نقطة الدخول للتطبيق (Application entry point)، تهيئة `Supabase`، و `ProviderScope`.
* **`lib/core/`**: الأدوات الأساسية (Core utilities)، تكوين الشبكة (network configuration)، تعيين الأخطاء (`error_mapper.dart`)، واجهة المستخدم للملاحظات السريرية (`clinical_feedback.dart`)، خدمات استنتاج الذكاء الاصطناعي (AI inference services)، قاعدة بيانات `Drift` المحلية، والمزامنة دون اتصال (offline sync).
* **`lib/data/models/`**: كيانات المجال (Domain entities) التي ترتبط مباشرة بمخططات `Postgres` (مثل `AppAlert`، `DailyReport`، `AiConversation`، `VitalAlert`).
* **`lib/data/repositories/`**: طبقات الوصول إلى البيانات المجردة (Abstracted data access layers) التي تتعامل مع استعلامات `Supabase` والتخزين المؤقت المحلي (local caching).
* **`lib/features/`**: منطق المجال الخاص بالميزات (Feature-specific domain logic) (مثل `Chatbot`، `Vitals Service`، التحقق من `X-Ray`).
* **`lib/presentation/controllers/`**: أجهزة التحكم في `Riverpod` (`StateNotifiers`/`Providers`) التي تدير حالة واجهة المستخدم (UI state) وتنسق استدعاءات الـ `repository`.
* **`lib/presentation/screens/`**: شاشات واجهة المستخدم مقسمة حسب الدور (Doctor, Patient, Companion, Facility, Auth).
* **`supabase/`**: التكوين الأساسي للواجهة الخلفية (Core backend configuration) بما في ذلك مخططات `SQL` (`schema.sql`)، سياسات الأمان على مستوى الصف (`policies.sql`)، والمشغلات (DB triggers).

---

## الجزء 1 — تحليل المشروع بالكامل

### نظرة عامة، الغرض، الأهداف
**VitaGuard** هو تطبيق هاتف محمول شامل متعدد الأدوار للإدارة السريرية والمراقبة عن بعد للمرضى. هدفه الأساسي هو سد الفجوة بين المراقبة المستمرة للمريض القائمة على الأجهزة والتدخل السريري الفوري.
**الأهداف**:
- توفير تنبيهات صحية في الوقت الفعلي للمتخصصين السريريين المعينين والمرافقين.
- تنفيذ استنتاج ذكاء اصطناعي على الجهاز (On-Device AI) للفحص السريع للأشعة السينية على الصدر (Chest X-Ray screening).
- توفير روبوت محادثة سريري (`Clinical Chatbot`) يعتمد على `LLM` للاستفسارات الطبية.
- ضمان فصل البيانات بشكل صارم من خلال التحكم في الوصول القائم على الدور (RBAC) في بنية سحابية أصلية (cloud-native architecture).

### الميزات
1. **مصادقة متعددة الأدوار (Multi-Role Authentication)**: المريض (Patient)، الطبيب (Doctor)، المرافق (Companion)، المنشأة (Facility).
2. **مراقبة المؤشرات الحيوية في الوقت الفعلي والتنبيهات (Real-time Vital Monitoring & Alerts)**: تتبع مباشر لمعدل ضربات القلب (`HR`)، نسبة الأكسجين (`SpO2`)، ودرجة الحرارة (`Temp`) مع تقييم للحدود المحلية وبث في الوقت الفعلي.
3. **استنتاج الذكاء الاصطناعي للأشعة السينية على الجهاز (On-Device X-Ray AI Inference)**: نموذج `DenseNet121 TFLite` لتقييم الالتهاب الرئوي (PNEUMONIA) مقابل الطبيعي (NORMAL) محلياً دون تأخير من الخادم (server latency).
4. **روبوت الدردشة السريري بالذكاء الاصطناعي (Clinical AI Chatbot)**: مساعد ذكاء اصطناعي واعي بالسياق (Context-aware) يستفيد من سجل المحادثة.
5. **قدرات العمل دون اتصال أولاً (Offline-First Capabilities)**: قاعدة بيانات `SQLite` محلية باستخدام `drift` متزامنة مع `Supabase`.
6. **إدارة المنشآت والمواعيد (Facility Management & Appointments)**: تتبع الاختبارات، السجلات، والحجوزات.

### البنية وهيكل المجلدات
يستخدم التطبيق **بنية متعددة الطبقات تعتمد على الميزات أولاً (Feature-First Layered Architecture)**:
- **الطبقة الأساسية (`lib/core/`)**: البنية التحتية (Infrastructure)، تعريفات قاعدة البيانات، ومعالجة الأخطاء الشاملة.
- **طبقة البيانات (`lib/data/`)**: النماذج (`DTOs`) والمستودعات (`Repositories`).
- **طبقة المجال (`lib/features/`)**: قواعد العمل (Business logic)، والقواعد، وتنقية استجابات الذكاء الاصطناعي (AI response sanitization).
- **طبقة العرض (`lib/presentation/`)**: الشاشات، عناصر واجهة المستخدم (`widgets`)، ومتحكمات `Riverpod`.

### مسار التنقل (Navigation Flow)
- يتم التعامل معه بشكل أساسي بواسطة `AuthGate` المرتبط بـ `authControllerProvider`.
- بناءً على دور المستخدم (`user['role']`)، يتم توجيه المستخدم فوراً إلى `MainPatient`، `MainDoctor`، `MainCompanion`، أو `MainFacility`.

### إدارة الحالة (State Management)
- **Riverpod**: يُستخدم على نطاق واسع عبر توليد الكود (`@riverpod`).
- **النمط (Pattern)**: يتم استخدام `AsyncValue` لحالات واجهة المستخدم غير المتزامنة (asynchronous UI states) (تحميل، خطأ، بيانات). يتم التعامل مع حقن التبعية (Dependency injection) بسلاسة من خلال الـ `providers` (مثل `ref.watch(supabaseServiceProvider)`).

### واجهة برمجة التطبيقات والواجهة الخلفية (API & Backend)
- **يعتمد على Supabase**: قاعدة بيانات `PostgreSQL`، مصادقة `Supabase Auth`، التخزين (Storage) (لبطاقات الهوية، سجلات المنشآت، والأشعة السينية)، ووظائف الحافة (Edge Functions) (لاستدعاء الـ `Chatbot` والربط الآمن).

### قاعدة البيانات و Supabase
- **قاعدة بيانات Postgres**: إعداد علائقي (Relational setup) يربط `profiles` بالفئات الفرعية (`patients`، `doctors`، `companions`، `facilities`).
- **الأمان على مستوى الصف (Row Level Security - RLS)**: يتم فرضه بصرامة (على سبيل المثال، يمكن للأطباء فقط قراءة بيانات مرضاهم المعينين).

### طبقات المستودع والخدمة (Repository & Service Layers)
- **المستودعات (Repositories)**: على سبيل المثال، `PatientRepository`، `AuthRepository` تقوم بتجريد استدعاءات `from('table')` الخاصة بـ `Supabase`.
- **الخدمات (Services)**: عمليات بحتة للأعمال/البنية التحتية (pure business/infrastructure operations) مثل `AlertTimerService` (حالة `Dart` بحتة) و `XrayInferenceService` (تنفيذ `TFLite`).

### معالجة الأخطاء (Error Handling)
- **ErrorMapper المركزي**: يلتقط `PostgrestException`، `AuthException`، والأخطاء الأصلية، ويحولها إلى رسائل `ClinicalErrorMessage` سهلة الاستخدام للمستخدم مرتبطة بـ `ClinicalErrorArea`.
- **انعكاس واجهة المستخدم (UI Reflection)**: يستخدم نظام تراكب (overlay system) متطوراً للملاحظات السريرية (`ClinicalFeedback`) لتنبيهات النجاح/التحذير/الحرجة بدلاً من استخدام الـ `snackbars` الخام.

---

## الجزء 2 — تفصيل الميزات

### 1. المراقبة والتنبيه للمؤشرات الحيوية في الوقت الفعلي
* **الملفات**: `alert_timer_service.dart`، `alert_center_provider.dart`، `alert_model.dart`، `alert_evaluation_engine.dart`.
* **الغرض**: يتتبع المقاييس المباشرة (live metrics) من الأجهزة (hardware)، ويحسب المتوسطات المتحركة (moving averages)، ويتحقق من الحدود السريرية (على سبيل المثال، `SpO2 < 92`)، ويطلق أحداثاً في الوقت الفعلي.
* **المنطق**: يقيم المقاييس على مدار تأخير بداية مدته 45 ثانية (لمنع التشويش). يجمع بين المتغيرات (على سبيل المثال، أكسجين منخفض + معدل ضربات قلب مرتفع = `CRITICAL`).
* **التحسينات**: دمج العزل في الخلفية (background isolates) لتحليل التدفقات الكبيرة من بيانات أجهزة الـ `BLE` مباشرة بدلاً من تقييمها على سلسلة عمليات واجهة المستخدم (UI-thread).

### 2. الذكاء الاصطناعي للأشعة السينية على الجهاز
* **الملفات**: `xray_inference_service_io.dart`، `xray_inference_service_web.dart`.
* **الغرض**: تحليل الأشعة السينية بشكل آمن وسريع.
* **المنطق**: يقوم بتغيير حجم الصور إلى `320x320`، ويطبق تسويات `ImageNet` داخلياً، ويقيم الـ `logits` الخام، ويطبق استقرار الـ `softmax`، ويتحقق من العتبة المُعايرة (`0.6` للالتهاب الرئوي).
* **المقايضات (Trade-offs)**: يوفر الاستنتاج المحلي النطاق الترددي (bandwidth) ويضمن الخصوصية، ولكنه يتطلب تضمين نموذج `.tflite` كبير الحجم (مما يؤدي إلى تضخم حجم التطبيق).

### 3. روبوت الدردشة بالذكاء الاصطناعي
* **الملفات**: `ai_chat_repository.dart`، `ai_response_sanitizer.dart`، `ai_chat_screen.dart`.
* **الغرض**: توفير ذكاء اصطناعي حواري سريري متخصص بناءً على دور المستخدم.
* **المنطق**: يحفظ مطالبات المستخدم في قاعدة البيانات، ويستدعي `Edge Function` الخاصة بـ `Supabase` (روبوت الدردشة)، ويستمع إلى الدفق (stream)، وينقي الاستجابة (عن طريق إزالة تسرب المطالبات الداخلية مثل علامات `<thought>` عبر `Regex`).

### 4. محرك المزامنة دون اتصال
* **الملفات**: `vitaguard_local_database.dart` (`Drift`)، `sync_queue_repository.dart`.
* **الغرض**: يضمن عمل التطبيق في حالة عدم الاتصال بالإنترنت.
* **المنطق**: يعترض الإجراءات، ويكتب في `SyncQueueItems`، ويدفع إلى `Supabase` عندما تبلغ أداة `Connectivity` عن استعادة الشبكة.

---

## الجزء 3 — مراجعة تقنية عميقة

### الشاشات، عناصر واجهة المستخدم (Widgets)، النماذج (Models)
- **AuthGate**: الموجه الأساسي (core router). يتجاوز شاشات البداية القياسية (splash screens) من خلال مراقبة تدفق `authControllerProvider`.
- **AppAlert**: نموذج يتم تحليله بعمق للتعامل مع الإكراه (coercion) من كائنات `JSON` الخاصة بالـ `payload` غير المهيكلة في الوقت الفعلي من `Supabase WebSockets`.

### المستودعات والخدمات (Repositories & Services)
- **`AuthRepository`**: تسجيل آمن للغاية. يستخدم بشكل ملحوظ تجاوز `RPC` يُسمى `link_companion_to_patient` لربط المرافقين بأمان قبل تعيين امتيازات القراءة الخاصة بهم في `RLS`.
- **`SupabaseService`**: يعمل كغلاف (wrapper) نمط المفرد (Singleton) يعترض جميع الاستعلامات للتسجيل/التتبع `track('query', ...)` لقياسات الأداء.

### إدارة الحالة واستدعاءات واجهة برمجة التطبيقات
- **تخزين Riverpod المؤقت**: يُستخدم `ref.keepAlive()` بكثافة للتخزين المؤقت لبيانات الـ `Auth` و الـ `AI Chat` لمنع ضغط الشبكة غير الضروري أثناء الانتقال بين الشاشات.

---

## الجزء 4 — مراجعة Supabase

### المصادقة والأمان على مستوى الصف (Auth & RLS)
- **مسار التسجيل (Signup Flow)**: ينشئ صفاً في `auth.users`. من المحتمل أن تتسلسل المشغلات (`Triggers`) المحددة في `SQL` لإنشاء الـ `profiles`. ثم يقوم التطبيق يدوياً بإنشاء الفئة الفرعية المحددة (`patients`، `doctors`).
- **سياسات الأمان على مستوى الصف (RLS Policies)**: دقيقة للغاية.
  - *مثال*: لا يمكن رؤية `patient_medical_history` إلا من قبل المريض (`public.is_owner()`)، أو طبيبه المعين (`public.assigned_doctor()`)، أو مرافقه المرتبط (`public.linked_companion()`).

### الوقت الفعلي (Realtime)
- **القنوات (Channels)**: يشترك التطبيق في مواضيع ديناميكية (dynamic topics) مثل `hw_vitals_$patientId`. هذا قابل للتوسع بشكل كبير حيث يستمع العملاء فقط إلى قنوات مرضى محددين بدلاً من الجداول العالمية (global tables).

### مخطط قاعدة البيانات (Database Schema)
- **النزاهة العلائقية (Relational Integrity)**: استخدام ممتاز للمفاتيح الخارجية (Foreign Keys). تتسلسل الـ `profiles` بحذف الـ `patients` و الـ `doctors`.
- **JSONB**: مُستخدم بذكاء في `medical_alerts` لحمولات بيانات الـ `alert_data` المرنة، لاستيعاب الإضافات المستقبلية للأجهزة.

---

## الجزء 5 و 6 — أسئلة المقابلة والإجابات النموذجية

### Flutter / Dart & Riverpod

**س1: لماذا اخترت Riverpod بدلاً من Bloc أو Provider؟**
> **الإجابة**: يوفر `Riverpod` أماناً في وقت الترجمة (compile-time safety)، ولا يعتمد على شجرة عناصر واجهة المستخدم (`Widget tree`) (لا يوجد خطأ `ProviderNotFoundException`)، ويجعل دمج الحالات غير المتزامنة (مثل انتظار المصادقة `Auth` وجلب بيانات المريض) تعريفياً عبر `AsyncValue`.

**س2: كيف يختلف `ConsumerStatefulWidget` عن `ConsumerWidget`؟**
> **الإجابة**: يتيح `ConsumerStatefulWidget` الحفاظ على حالات دورة حياة عناصر واجهة المستخدم المحلية (local widget lifecycle states) (مثل `initState` أو `AnimationControllers`) مع الاستمرار في الوصول بسهولة إلى الـ `providers` باستخدام `ref.read` أو `ref.watch` داخل فئة الحالة (state class).

**س3: كيف تمنع تسرب الذاكرة (memory leaks) عند الاستماع إلى الـ Streams في Riverpod؟**
> **الإجابة**: باستخدام `ref.onDispose(() { stream.cancel(); })`. عندما لا يتم مراقبة الـ `provider` من قبل أي `widget`، يقوم `Riverpod` تلقائياً بالتخلص منه واستدعاء الـ `callbacks` المسجلة للتخلص (dispose)، مما ينظف المستمعين (listeners).

**س4: اشرح استخدام ملفات `.g.dart`.**
> **الإجابة**: هذه ملفات تم إنشاؤها تلقائياً بواسطة `build_runner`. بالنسبة لـ `Riverpod`، فإنها تحول الدوال المشروحة بـ `@riverpod` إلى فئات `Provider` ملموسة (concrete). بالنسبة لـ `Drift`، فإنها تنشئ فئات تخطيط (mapping) لجداول `SQL`. إنها تقلل من الأكواد المكررة (boilerplate) وتضمن أمان النوع (type safety).

**س5: ما هو `AsyncValue` ولماذا هو مفيد؟**
> **الإجابة**: يُعد `AsyncValue` فئة اتحاد (union class) في `Riverpod` تمثل حالة عملية غير متزامنة (`data`, `loading`, `error`). إنه يجبر المطور على التعامل مع جميع الحالات الثلاث في واجهة المستخدم باستخدام `.when()`، مما يمنع الاستثناءات غير المعالجة (unhandled exceptions) أو أشرطة التحميل اللانهائية (infinite loaders).

**س6: في `AuthGate`، تستخدم `ref.watch()`. ماذا يحدث إذا تغيرت الحالة؟**
> **الإجابة**: يقوم `ref.watch` بإنشاء تبعية تفاعلية (reactive dependency). إذا تغيرت حالة `authControllerProvider` (على سبيل المثال، خروج المستخدم)، يعيد `AuthGate` البناء تلقائياً ويُرجع `OnboardingScreen`، مما يزيل شجرة عناصر واجهة المستخدم (widget tree) الموثقة الحالية على الفور.

### البنية والأنماط (Architecture & Patterns)

**س7: اشرح نمط المستودع (Repository Pattern) كما تم تنفيذه في مشروعك.**
> **الإجابة**: تقوم المستودعات (مثل `PatientRepository`) بتجريد مصدر البيانات (`Supabase` أو `Drift`). لا تطلب واجهة المستخدم / المتحكمات من المستودع سوى `List<DailyReport>`. إنهم لا يعرفون ما إذا كانت البيانات جاءت من ذاكرة التخزين المؤقت المحلية لـ `SQLite` أو من السحابة. هذا يفصل منطق العمل (business logic) عن كود الشبكة.

**س8: لماذا تفصل النماذج (Models) عن عناصر واجهة المستخدم (UI Widgets)؟**
> **الإجابة**: إنه يتبع مبادئ `SOLID` (المسؤولية الفردية - Single Responsibility). تمثل النماذج البيانات فقط ومنطق التحويل إلى `JSON` (`toMap()`, `fromMap()`). عناصر واجهة المستخدم (Widgets) تتعامل مع العرض (rendering) فقط. هذا يجعل النماذج قابلة لإعادة الاستخدام وقابلة لاختبار الوحدة (unit-testable) دون بيئة `Flutter`.

**س9: ما هو حقن التبعية (dependency injection) وكيف يتم تحقيقه هنا؟**
> **الإجابة**: حقن التبعية هو تمرير التبعيات إلى الفئات (classes) بدلاً من أن تقوم بتكوينها (instantiate) بداخلها. هنا، `patientRepository(Ref ref)` يقوم بحقن `SupabaseService`، و `LocalCacheRepository`، و `SyncQueueRepository` في `PatientRepository`، مما يسمح بالسخرية (mocking) السهلة أثناء الاختبارات.

### Supabase وقاعدة البيانات

**س10: ما هو الأمان على مستوى الصف (RLS) ولماذا هو أمر بالغ الأهمية في VitaGuard؟**
> **الإجابة**: يضمن `RLS` تصفية استعلامات قاعدة البيانات تلقائياً على مستوى محرك `Postgres` بناءً على المستخدم المُصادق عليه. في `VitaGuard`، فإنه يضمن عدم تمكن الطبيب من الاستعلام عن طريق الخطأ أو عن قصد عن مريض لم يتم تعيينه له، وهو مطلب بالغ الأهمية للامتثال الطبي ولـ `HIPAA`.

**س11: كيف تتعامل مع تحميل ملفات الأشعة السينية (X-Rays)؟**
> **الإجابة**: نحن نستخدم `Supabase Storage`. يتم تحميل الملف إلى دلو `patient_documents` (bucket) باستخدام `supabase.storage.from().uploadBinary()`. بعد ذلك، يتم حفظ مسار الملف المُرجع في قاعدة بيانات `Postgres`.

**س12: اشرح كيف يتم استخدام Realtime WebSockets في نظام التنبيه.**
> **الإجابة**: يشترك التطبيق في تغييرات `Postgres` على جدول `patient_live_vitals` باستخدام `client.channel().onPostgresChanges()`. عندما يقوم الجهاز (hardware) بإدراج صف جديد، يقوم `Supabase` ببثه إلى التطبيق على الفور، متجاوزاً الاقتراع (HTTP polling) وموفراً في البطارية/النطاق الترددي.

**س13: لماذا تستخدم وظيفة RPC (`link_companion_to_patient`) لتسجيل المرافقين؟**
> **الإجابة**: لا يمتلك المرافق الذي يقوم بالتسجيل صلاحية الوصول للقراءة في جدول المريض بعد بسبب `RLS`. من خلال تنفيذ وظيفة `RPC` باستخدام `security definer`، تعمل الوظيفة بامتيازات `Postgres` مرتفعة، وتتحقق بأمان من `companion_code`، وتقوم بإدراج رابط العلاقة بشكل آمن على الخادم.

### تكامل الذكاء الاصطناعي وتعلم الآلة (AI & Machine Learning Integration)

**س14: لماذا تقوم بتنفيذ استنتاج الأشعة السينية (X-Ray Inference) على الجهاز (TFLite) بدلاً من عبر API؟**
> **الإجابة**: الخصوصية الطبية، ردود الفعل بدون زمن انتقال (zero-latency)، وإمكانيات عدم الاتصال بالإنترنت. يستهلك رفع صور الأشعة السينية الثقيلة النطاق الترددي (bandwidth). يعالج الاستنتاج المحلي عبر `tflite_flutter` الصورة على الفور ويبقي المعلومات الصحية الشخصية (PHI) على جهاز المستخدم بصرامة حتى يختار مزامنتها.

**س15: اشرح تنفيذ softmax في `_toProbs`.**
> **الإجابة**: يخرج نموذج `DenseNet121` مخرجات `logits` خام غير محدودة. نحن نطبق وظيفة `softmax` الرياضية، باستخدام الطرح الأقصى للاستقرار العددي (`logit - max_logit`)، لتحويل هذه الأرقام الخام إلى احتمالات (probabilities) يصل مجموعها إلى 1.0 (0% إلى 100%).

**س16: كيف يحتفظ روبوت الدردشة (AI Chatbot) بسياق المحادثة؟**
> **الإجابة**: يخزن التطبيق سجل الرسائل في جدول `ai_messages`. عند استدعاء وظيفة `Supabase Edge Function`، نمرر `conversationId`. تقرأ الوظيفة السجل من قاعدة البيانات، وتقوم بتنسيقه في حمولة مطالبة (prompt payload) خاصة بـ `OpenAI/Gemini`، وتُرجع الاستجابة السياقية (contextual response).

### الأداء والمزامنة دون اتصال (Performance & Offline Synchronization)

**س17: كيف تتعامل قاعدة بيانات Drift المحلية مع وضع عدم الاتصال (offline mode)؟**
> **الإجابة**: يعترض التطبيق عمليات القراءة/الكتابة. إذا كان غير متصل، يتم حفظ عمليات الكتابة في جدول `Drift` المسمى `SyncQueueItems`. تراقب خدمة خلفية مكتبة `ConnectivityPlus`. عند استعادة الإنترنت، فإنها تكرر عملية الطابور (queue) وتقوم بتدفق (flush) البيانات إلى `Supabase`.

**س18: ما هو الغرض من متغيرات التهدئة (cooldown variables) في `AlertTimerService`؟**
> **الإجابة**: إنها تمنع الإرهاق من كثرة التنبيهات (alert fatigue). إذا كان الأكسجين (`SpO2`) للمريض يحوم حول الحد المعين (على سبيل المثال، 91%، 92%، 91%)، فقد يؤدي ذلك إلى تشغيل المئات من التنبيهات. تضمن التهدئة أنه بعد التشغيل، لن يقوم مقياس محدد بتشغيل إشعار آخر حتى يمر وقت معين (على سبيل المثال، 45 ثانية).

### الكود وآليات السلامة (Code & Safety Mechanics)

**س19: اشرح منطق `AiResponseSanitizer`.**
> **الإجابة**: تقوم نماذج اللغات الكبيرة (LLMs) أحياناً بـ "تسريب" مطالبات النظام الداخلية (internal system prompts) أو عمليات التفكير (على سبيل المثال، `<thought>I should act as a doctor</thought>`). يستخدم المُعقم `Regex` لمسح هذه العلامات والعبارات المحظورة لضمان رؤية المريض لاستجابة سريرية ومهنية خالصة دون تعليمات مهلوسة (hallucinated instructions).

**س20: كيف يقوم `ErrorMapper` بتحسين تجربة المستخدم؟**
> **الإجابة**: الاستثناءات الخام (Raw exceptions) (مثل انتهاك المفتاح الخارجي `23503` الخاص بـ `Postgres`) تربك المستخدمين. يعترض `ErrorMapper` هذه الأخطاء، ويتحقق من السياق (`ClinicalErrorArea`)، ويترجمها إلى رسائل واجهة مستخدم ذات سياق طبي قابلة للتنفيذ مثل *"يتم إعداد ملفك الشخصي حالياً."*

---

## الجزء 7 — الأسئلة المعتمدة على الكود

**1. الملف:** `alert_evaluation_engine.dart`
* **المرجع في الكود:** `_windows[key]!.add(val); if (_windows[key]!.length > 5) _windows[key]!.removeAt(0);`
* **السؤال:** ماذا يفعل هذا المنطق ولماذا هو ضروري سريرياً؟
* **الإجابة:** إنه ينفذ مرشح المتوسط المتحرك (Moving Average filter) (نافذة منزلقة بحجم 5). تنتج أجهزة الاستشعار (hardware sensors) ارتفاعات مفاجئة ومشوشة. يقوم حساب متوسط آخر 5 قراءات بتنعيم القطع الأثرية (artifacts) قبل التقييم مقابل الحدود الحرجة، مما يقلل بشكل كبير من تنبيهات الإيجابية الخاطئة (false positive alerts).
* **سؤال متابعة متقدم:** كيف ستتعامل مع موقف يرسل فيه المستشعر `null` أو قيماً متطرفة بشكل كبير (على سبيل المثال، Temp = 0) داخل هذه النافذة؟ (الإجابة: أضف رفضاً للقيم المتطرفة أو فحوصات للانحراف المعياري قبل الإضافة إلى النافذة).

**2. الملف:** `auth_provider.dart`
* **المرجع في الكود:** `await _waitForSessionRestoration();` في `_init()`
* **السؤال:** لماذا توجد طريقة محددة لانتظار استعادة الجلسة (session restoration)؟
* **الإجابة:** تقوم مصادقة `Supabase Auth` بتخزين رمز `JWT` المميز في تخزين آمن. يعد استرداده عملية غير متزامنة (asynchronous). إذا تحقق التطبيق فوراً من `currentSession` عند التشغيل، فقد يُرجع القيمة فارغة (`null`) لأن القراءة لم تنته، مما يتسبب في تسجيل خروج خاطئ. هذه الدالة تنتظر ما يصل إلى ثانيتين لحزمة الـ `SDK` لاستعادة الجلسة.

**3. الملف:** `schema.sql`
* **المرجع في الكود:** `id uuid primary key references profiles(id) on delete cascade`
* **السؤال:** في جدول `patients`، لماذا يشير المعرف `id` إلى `profiles(id)` بدلاً من `auth.users`؟
* **الإجابة:** هذا تصميم مُسوى (Normalized design - توريث الجداول). تتم إدارة `auth.users` بواسطة `Supabase`. يحتفظ `profiles` بالبيانات المشتركة الخاصة بالتطبيق (الاسم، الهاتف). يحتفظ `patients` بالبيانات الخاصة بالدور. من خلال استخدام التسلسل (`cascade`) من `profiles` إلى `patients`، فإن حذف الملف الشخصي يمسح البيانات المتخصصة بشكل نظيف.

---

## الجزء 8 — قرارات التصميم

### 1. بنية المجلدات: الميزات أولاً مقابل الطبقات أولاً (Feature-First vs Layer-First Folder Structure)
* **القرار**: قمنا بتجميع الملفات حسب الميزة (`lib/features/chatbot`، `lib/features/vitals`) بدلاً من التقسيم الصارم حسب النوع (`lib/screens`، `lib/models`).
* **الإيجابيات**: قابل للتطوير بدرجة كبيرة. إذا عمل المطور على الـ `Chatbot`، فكل شيء (البيانات، المجال، واجهة المستخدم) موجود في مجلد واحد. أسهل في حذف أو عزل الميزات.
* **السلبيات**: قد يصبح من الصعب وضع عناصر واجهة المستخدم المشتركة أو النماذج (ومنه جاءت مجلدات `lib/core` أو `shared`).

### 2. استخدام Riverpod لإدارة الحالة
* **القرار**: تم استبدال `setState` أو `Provider` القياسي بـ `Riverpod`.
* **الإيجابيات**: أمان في وقت الترجمة (Compile-time safety). توليد الكود (`@riverpod`) يلغي الأكواد المكررة (boilerplate). إمكانية الوصول العالمية دون الحاجة لـ `BuildContext`.
* **السلبيات**: منحنى تعلم (learning curve) أكثر حدة. يعتمد بكثافة على إنشاءات `build_runner`.

### 3. الذكاء الاصطناعي على الجهاز مقابل الذكاء الاصطناعي السحابي للأشعة السينية (On-Device AI vs Cloud AI for X-Rays)
* **القرار**: تم نشر نموذج `TFLite` داخل التطبيق بدلاً من استدعاء `Python REST API`.
* **الإيجابيات**: يعمل بدون إنترنت. نتائج فورية. أرخص (لا توجد تكاليف لخادم معالجة الرسومات `GPU`). متوافق تماماً مع قوانين خصوصية البيانات (الصور لا تغادر الهاتف للفحص الأولي).
* **السلبيات**: يزداد حجم التطبيق بحوالي 20-50 ميغابايت. لا يمكن تحديث النموذج ديناميكياً دون تحديث للتطبيق في المتجر (app store update).

### 4. Supabase بدلاً من Firebase
* **القرار**: تم الانتقال إلى `Supabase`.
* **الإيجابيات**: تتعامل قاعدة البيانات العلائقية `Postgres` محلياً مع الروابط السريرية المعقدة (Doctors -> Patients -> Vitals) بشكل أفضل بكثير من هياكل مستندات `NoSQL`. وظائف `SQL RPC` مدمجة.
* **السلبيات**: يتطلب معرفة قوية بـ `SQL`. قد يكون من الصعب تصحيح أخطاء الأمان على مستوى الصف (`RLS`) مقارنة بقواعد `Firebase`.

---

## الجزء 9 — نقاط الضعف والدفاع

### 1. حجم التطبيق بسبب نماذج الذكاء الاصطناعي
* **النقد:** يجعل نموذج `.tflite` حجم ملف الـ `APK/IPA` كبيراً جداً.
* **الدفاع:** على الرغم من صحة ذلك، إلا أن المقايضة هي توفير تشخيصات منقذة للحياة بدون تأخير والامتثال الصارم لـ `HIPAA`.
* **التحسين:** في الإصدار 2 (V2)، يمكننا تنفيذ تنزيل الموارد عند الطلب (On-Demand Resource downloading) (جلب نموذج الذكاء الاصطناعي من `Supabase Storage` عند الاستخدام الأول بدلاً من تجميعه داخل التطبيق).

### 2. الاعتماد على الشبكة لعمل روبوت الدردشة (Chatbot)
* **النقد:** إذا كان المستخدم غير متصل بالإنترنت، فسيفشل الـ `AI Chatbot`.
* **الدفاع:** تتطلب نماذج `LLMs` قوة حوسبة هائلة غير متوفرة في الأجهزة المحمولة الحالية. يلتقط التطبيق أخطاء الشبكة بأمان ويبلغ المستخدم بها.
* **التحسين:** التخزين المؤقت (Cache) للأسئلة الشائعة المُحددة مسبقاً محلياً.

### 3. استنزاف البطارية في الوقت الفعلي
* **النقد:** ستؤدي الاتصالات المستمرة (`WebSockets`) لـ (`patient_live_vitals`) إلى استنزاف البطارية على جهاز الطبيب.
* **الدفاع:** الاشتراكات مرتبطة بدورة حياة عنصر واجهة المستخدم (Widget lifecycle). عندما يضع الطبيب التطبيق في الخلفية، يتوقف الاتصال مؤقتاً.
* **التحسين:** الاستفادة من `Firebase Cloud Messaging` (`FCM`) عبر خطافات ويب `Supabase webhooks` لتنشيط الجهاز **فقط** عند اختراق حد التنبيه، بدلاً من بث جميع المؤشرات الحيوية الطبيعية إلى هاتف الطبيب.

---

## الجزء 10 — أهم الأسئلة

### 5 أسئلة أساسية
1. كيف تفصل البنية واجهة المستخدم (UI) عن منطق العمل (Business Logic)؟
2. اشرح كيف ينتقل التطبيق بين حالات الاتصال بالإنترنت وعدم الاتصال به.
3. وجهني عبر تنفيذ الكود عندما يطلق جهاز استشعار الأجهزة (hardware sensor) تنبيهاً حرجاً "Critical SpO2".
4. كيف يتم تأمين بيانات المستخدم بحيث لا يمكن للمريض (أ) قراءة بيانات المريض (ب)؟
5. صِف دورة حياة معالجة صورة الأشعة السينية عبر `TFLite` من الكاميرا وحتى النتيجة.

### 5 أسئلة تقنية متقدمة
1. اشرح الآلية الموجودة داخل `xray_inference_service_io.dart` التي تمنع تسرب الذاكرة (memory leaks) عند تخصيص الموترات (allocating tensors).
2. كيف تؤثر تعليمة Riverpod المكتوبة `@Riverpod(keepAlive: true)` على تجميع البيانات المهملة (garbage collection) للحالة؟
3. صِف ضعف حقن مطالبات نماذج اللغة (LLM Prompt Injection) وكيف يخفف `AiResponseSanitizer` من ذلك.
4. ماذا يحدث في طابور مزامنة `Drift` (`SyncQueue`) إذا تعارضت عملية كتابة في قاعدة البيانات مع طابع زمني أحدث على الخادم؟
5. كيف يتعامل `Supabase` مع تجميع الاتصالات (connection pooling) عندما تفتح آلاف الأجهزة اتصالات `Realtime WebSocket`؟

---

## الجزء 11 — الدليل النهائي للدراسة

**قائمة المراجعة قبل المناقشة:**
- [ ] **البنية (Architecture)**: افهم كيف يستدعي الـ `Controller` الـ `Repository`، والذي يستدعي بدوره `Supabase/Drift`. كن قادراً على رسم هذا المسار.
- [ ] **قاعدة البيانات (Database)**: احفظ علاقات المخطط (`profiles` -> `patients` -> `assigned_doctor_id`).
- [ ] **الأمان (RLS)**: كن مستعداً لشرح منطق `SQL` الدقيق الذي يحمي خصوصية المريض.
- [ ] **تكامل الذكاء الاصطناعي (AI Integration)**: افهم مصطلحات *Logits*، *Softmax*، *Image Normalization*، و *Tensor*.
- [ ] **الحالة (State)**: اشرح `AsyncValue.when(data, error, loading)`.
- [ ] **التطبيق في العالم الحقيقي (Real-world Application)**: كن مستعداً للإجابة على سؤال "ماذا لو تم استخدام هذا في مستشفى حقيقي اليوم؟" ركز على منطق المتوسط المتحرك لـ `ErrorMapper` و `AlertEvaluationEngine` كشبكات أمان رئيسية لديك.

*تم الإنشاء بواسطة المساعد الذكي لـ VitaGuard.*
